# Used by script/audit-production.sh, script/backup-production.sh and
# script/sync-production.sh. Copy to .env and fill in.
PROD_HOST=your-production-server.com
PROD_USER=deploy
PROD_DB=blackbook_production
PROD_APP_PATH=/var/www/blackbook
LOCAL_DB=blackbook_dev

# script/resync-beta.sh defaults to the current hosts. Override if they move.
# BETA_HOST=
