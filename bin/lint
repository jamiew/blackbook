#!/usr/bin/env bash
# Ruby linting script
set -e

echo "🔍 Running RuboCop linter..."
# No --fail-level: match CI (.github/workflows/rubocop.yml), which fails on
# convention offenses too. A weaker local gate hides CI failures.
bundle exec rubocop --format simple

echo "✅ Linting passed!"