- content_for :header_nav do
  - if current_user
    %li= link_to 'my account', account_path
    %li= link_to 'logout', logout_path, :class => 'logout'
  - else
    %li= link_to 'login', login_path
    %li= link_to 'register', signup_path

!!! XML
!!!
%html{html_attrs}
  %head
    = render :partial => 'layouts/common_headers'
    %title= page_title
  = render :partial => 'layouts/body'
  = remote_javascript_includes
  - bundle do
    = local_javascript_includes
  = blackbird_tags
  = render :partial => 'layouts/on_ready'
