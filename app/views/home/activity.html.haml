


%h2 All Activity On This Internet Website
%p
  &larr;
  = link_to "back to home", root_path

%p
  %br
  - if @notifications.blank?
    = "None yet."
  - else
    %ol.square
      - @notifications.each do |notif|
        %li
          = time_ago_in_words(notif.created_at.strftime('%D %T')).gsub('about ','')
          ago
          == &mdash;
          - begin
            - case notif.subject
            - when Tag
              - tag = notif.subject
              - user = tag.user
              = link_to "tag #{tag.id}".inspect, url_for(notif.subject)          
              == created by #{link_to(user.login, user_path(user)) rescue '(niluser)'}
          
            - when User
              - user = notif.subject            
              = link_to user.login, user_path(user)
              signed up
          - rescue
            == ERROR: #{$!}
          